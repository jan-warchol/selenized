Selenized color palette for Alacritty
===============================

Installation
------------

Replace the `colors` key in `~/.config/alacritty/alacritty.yml` with one of the color schemes in this folder.
